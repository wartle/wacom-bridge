# Build output
bin/
obj/
publish/
*.user
.vs/

# Wacom STU SDK binaries are proprietary; copy them from your SDK install (see README).
WacomStuMouseBridge/lib/*.dll

# Local calibration (screen positions are specific to each PC)
wacom-bridge.conf

# Release builds (upload the zip to GitHub Releases instead)
release/
